# QuantMind应用虚拟主机配置
# 此文件可以根据具体域名需求进行自定义

# 示例：特定域名配置
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com www.your-domain.com;
#
#     # SSL证书配置（如果有特定域名证书）
#     ssl_certificate /etc/nginx/ssl/your-domain.com/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/your-domain.com/key.pem;
#
#     # 其他配置继承主配置文件
#     include /etc/nginx/snippets/quantmind-common.conf;
# }

# 默认配置已在主配置文件中定义
# 此文件用于扩展特定域名或特殊路由配置